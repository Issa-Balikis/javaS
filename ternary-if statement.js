//Ternary If Statement
//you can have  more than one constion within the ternary if statement, but it is not advisable because it can be hard to debug.
var number = 13;
var result = number % 2 == 0 ? 'Even' : 'Odd'
console.log(result);


/* if (number % 2 == 0) {
    console.log('even');
}else {
    console.log('odd');
} */
