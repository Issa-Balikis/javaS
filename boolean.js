//Boolean, i.e, true or false.

var isAdult = false;

if (isAdult) {
    console.log("is adult :)")
} else {
    console.log("is not an adult :(")
}
//we can flip the values with boolean
console.log(!true)
console.log(!false)