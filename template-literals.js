// Template Literals; allows you to use a much more elegant syntax when combining values with strings.

const name = 'kim';
const age = '30';
const country = 'Nigeria';

console.log(`name ${name.length} age ${age} country ${country}`)